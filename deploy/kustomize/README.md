# Being retired

This directory is the cluster configuration of the maintainer's homelab and is
in the middle of moving to a **private** repository — see the GitOps section of
[docs/deploying.md](../../docs/deploying.md) for why an application repo should
not be the thing a cluster syncs from.

**Do not send pull requests against these files, and do not use them as the
example.** The supported, vendor-neutral example is [`../kubernetes/`](../kubernetes).
