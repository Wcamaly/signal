apiVersion: v1
kind: ConfigMap
metadata:
  name: signal-config
data:
  NODE_ENV: production
  PORT: "3000"
  SIGNAL_DATA_DIR: /app/data
  # The CronJob schedule is read in this timezone too — keep both in sync.
  TZ: UTC
